import { useState } from "react";
import { ArrowRight, ArrowUpRight, Hexagon, MapPin, Layers, ScanLine, Sparkles } from "lucide-react";
import { listCities } from "@/lib/sitescope/mock-data";
import type { City } from "@/lib/sitescope/types";
export function Welcome({onStart,onDemo}:{onStart:(c:City)=>void;onDemo:(c:City)=>void}) {
 const [cityId,setCityId]=useState("ahmedabad");const cities=listCities();const city=cities.find(c=>c.id===cityId)!;
 return <div className="welcome-page"><header className="welcome-header"><span className="brand"><span className="brand-symbol"><Hexagon size={23}/><span/></span>SiteScope<span className="brand-dot">.</span></span><span className="welcome-nav">Better places. Better possibilities.</span><button className="button small" onClick={()=>onDemo(city)}>Explore the demo <ArrowUpRight size={15}/></button></header><main className="welcome-main"><div className="welcome-copy"><span className="eyebrow"><i className="status-dot"/> LOCATION INTELLIGENCE, MADE HUMAN</span><h1>Your next big idea.<br/>Its perfect <em>place.</em></h1><p>Turn a city of possibilities into a shortlist you can trust. Find, compare and stress-test your next business location in one clear view.</p><div className="welcome-start"><div className="city-picker"><MapPin size={18}/><select aria-label="Choose your city" value={cityId} onChange={e=>setCityId(e.target.value)}>{cities.map(c=><option key={c.id} value={c.id}>{c.name}, Gujarat</option>)}</select></div><button className="button primary" onClick={()=>onStart(city)}>Find my location <ArrowRight size={17}/></button></div><p className="welcome-footnote">One city. Nine spatial layers. A clearer decision.<br/>Explore with a clearly labeled synthetic dataset.</p></div><div className="welcome-visual"><div className="illustration-grid">{Array.from({length:77},(_,i)=><span key={i} style={{background:["#dce8d7","#b8d2be","#86b59c","#56997b","#e1d7b6"][(i*17+i%5)%5],opacity:0.4+(i%4)*0.15}}/>)}</div><div className="visual-river"/><span className="visual-road one"/><span className="visual-road two"/><span className="visual-road three"/><div className="floating-site"><span className="site-pin active">1</span><div><small>YOUR NEXT OPPORTUNITY</small><strong>A place with potential.</strong><p>Demand. Access. Room to grow.</p></div><Sparkles size={19}/></div><div className="visual-caption"><span className="status-dot"/>From a whole city to your right location.</div><div className="visual-coordinate">23.0225° N<br/>72.5714° E</div></div></main><div className="welcome-steps"><div><Layers size={21}/><span><strong>01. See the whole picture</strong><p>Bring demand, access and risk together.</p></span></div><div><ScanLine size={21}/><span><strong>02. Find your best fit</strong><p>Rank locations around your priorities.</p></span></div><div><Sparkles size={21}/><span><strong>03. Explore what happens next</strong><p>Test a change before making your move.</p></span></div></div><footer className="welcome-footer"><span>SiteScope / Geospatial Site Readiness Analyzer</span><span>Built for possibilities.</span></footer></div>;
}
